WERSS_PORT=8001
# Fake local-only example. Replace the password in the ignored .env file.
# Percent-encode URL-reserved characters in the real password.
DB=mysql+pymysql://werss_app:local-example-secret@host.docker.internal:3306/werss?charset=utf8mb4
