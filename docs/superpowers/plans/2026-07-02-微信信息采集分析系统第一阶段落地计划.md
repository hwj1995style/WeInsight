# 微信信息采集分析系统第一阶段 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地第一阶段可运行闭环：项目骨架、配置、MySQL 初始化、双链路任务表、UI 锁租约、群/文章链路调度骨架、可恢复文章进度和测试验证。

**Architecture:** 使用 Python 3.11 单仓库实现，按 group/article 两条 pipeline 分包，数据库表和任务状态完全隔离。微信 UI 操作通过 `wechat_ui_lock` 串行化，文章链路只占用微信 UI 获取链接，正文解析在释放 UI 锁后执行。

**Tech Stack:** Python 3.11, SQLAlchemy 2.x, PyMySQL, PyYAML, pytest, freezegun, APScheduler, Playwright, wxauto/pywinauto interface adapters.

---

## 文件结构

第一阶段创建以下文件：

```text
WeInsight/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py
│   ├── domain/
│   │   ├── __init__.py
│   │   ├── hashes.py
│   │   └── desensitize.py
│   ├── storage/
│   │   ├── __init__.py
│   │   ├── db.py
│   │   ├── schema.py
│   │   └── lock_repo.py
│   ├── rpa/
│   │   ├── __init__.py
│   │   ├── interfaces.py
│   │   └── fake_clients.py
│   └── pipelines/
│       ├── __init__.py
│       ├── group_pipeline.py
│       └── article_pipeline.py
├── config/
│   └── config.dev.yaml
├── sql/
│   └── init.sql
├── tests/
│   ├── conftest.py
│   ├── test_config.py
│   ├── test_hashes.py
│   ├── test_desensitize.py
│   ├── test_ui_lock.py
│   ├── test_article_state_machine.py
│   └── test_pipeline_isolation.py
├── requirements.txt
└── README.md
```

每个文件职责：

- `app/core/config.py`：读取 YAML 和环境变量，校验微信版本、数据库、pipeline、UI 锁配置。
- `app/storage/schema.py`：读取 `sql/init.sql`，供 CLI 和测试复用。
- `app/storage/lock_repo.py`：实现 `wechat_ui_lock` 获取、续约、释放、僵死锁恢复。
- `app/rpa/interfaces.py`：定义 RPA 抽象接口。
- `app/rpa/fake_clients.py`：提供测试和开发用 fake RPA 客户端，不操作真实微信。
- `app/pipelines/group_pipeline.py`：群链路调度骨架，读取群任务、获取 UI 锁、调用 group RPA、入库、释放锁。
- `app/pipelines/article_pipeline.py`：文章链路状态机，空窗判断、按账号分片拿链接、释放 UI 锁后解析。

---

## 测试与回归总策略

第一阶段全部任务按测试先行推进。

必须覆盖：

```text
配置读取和默认值
hash 生成稳定性
手机号和微信号脱敏
MySQL 表结构初始化
group/article 任务表隔离
wechat_ui_lock 获取、续约、释放、过期恢复
文章链路空窗判断
文章链路被核心群打断后的进度恢复
文章正文解析不持有 UI 锁
```

本阶段不接真实微信 PC UI。真实微信 RPA 只在后续 POC 任务中接入，第一阶段使用 fake RPA 客户端验证调度和数据状态。

---

### Task 1: Project Skeleton And Dependencies

**Files:**
- Create: `requirements.txt`
- Create: `README.md`
- Create: `app/__init__.py`
- Create: `app/core/__init__.py`
- Create: `app/domain/__init__.py`
- Create: `app/storage/__init__.py`
- Create: `app/rpa/__init__.py`
- Create: `app/pipelines/__init__.py`
- Create: `tests/conftest.py`

- [ ] **Step 1: Create dependency file**

Create `requirements.txt`:

```text
APScheduler==3.10.4
PyMySQL==1.1.1
PyYAML==6.0.2
SQLAlchemy==2.0.32
freezegun==1.5.1
pytest==8.3.2
python-dotenv==1.0.1
playwright==1.46.0
```

- [ ] **Step 2: Create package marker files**

Create empty package files:

```text
app/__init__.py
app/core/__init__.py
app/domain/__init__.py
app/storage/__init__.py
app/rpa/__init__.py
app/pipelines/__init__.py
```

- [ ] **Step 3: Create pytest fixture file**

Create `tests/conftest.py`:

```python
from __future__ import annotations

import os

import pytest


@pytest.fixture(autouse=True)
def default_env(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("WEINSIGHT_MYSQL_PASSWORD", "weinsight_dev")
    monkeypatch.setenv("WEINSIGHT_ENV", "test")
```

- [ ] **Step 4: Create README**

Create `README.md`:

```markdown
# WeInsight

微信信息采集分析系统。

第一阶段目标：

- 使用开发库 `weinsight_dev`
- 建立 group/article 双链路隔离骨架
- 使用 fake RPA 验证调度、任务状态和 UI 锁
- 后续 POC 再接入微信 PC 4.1.10.31

## 开发数据库

```yaml
host: 127.0.0.1
port: 3307
database: weinsight_dev
username: weinsight
password: WEINSIGHT_MYSQL_PASSWORD
```
```

- [ ] **Step 5: Install dependencies**

Run:

```powershell
python -m pip install -r requirements.txt
```

Expected: command exits with code `0`.

- [ ] **Step 6: Run empty test suite**

Run:

```powershell
pytest -q
```

Expected: pytest starts successfully and reports no collected tests or passing tests.

---

### Task 2: Configuration Loader

**Files:**
- Create: `config/config.dev.yaml`
- Create: `app/core/config.py`
- Test: `tests/test_config.py`

- [ ] **Step 1: Write failing config tests**

Create `tests/test_config.py`:

```python
from __future__ import annotations

from pathlib import Path

from app.core.config import load_config


def test_load_config_expands_mysql_password() -> None:
    config = load_config(Path("config/config.dev.yaml"))

    assert config.mysql.host == "127.0.0.1"
    assert config.mysql.port == 3307
    assert config.mysql.database == "weinsight_dev"
    assert config.mysql.password == "weinsight_dev"


def test_pipeline_capacity_defaults_match_design() -> None:
    config = load_config(Path("config/config.dev.yaml"))

    assert config.wechat.pc_version == "4.1.10.31"
    assert config.pipelines.group.core_group_limit == 5
    assert config.pipelines.article.account_limit == 20
    assert config.pipelines.article.max_accounts_per_ui_slice == 1
    assert config.pipelines.ui_resource.max_core_group_block_seconds == 10
    assert config.pipelines.ui_resource.lock_lease_seconds == 120
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```powershell
pytest tests/test_config.py -q
```

Expected: FAIL with `ModuleNotFoundError` or missing `load_config`.

- [ ] **Step 3: Create development config**

Create `config/config.dev.yaml`:

```yaml
app:
  env: dev
  timezone: Asia/Shanghai
  log_level: INFO

wechat:
  pc_version: "4.1.10.31"
  window_title: "微信"
  fixed_window: true
  check_login_interval_seconds: 300

pipelines:
  group:
    enabled: true
    core_group_limit: 5
    poll_interval_seconds: 30
    max_group_per_round: 5
    rpa_timeout_seconds: 60
    target_round_seconds: 60
  article:
    enabled: true
    account_limit: 20
    crawl_time: "08:30"
    max_articles_per_account: 5
    max_accounts_per_ui_slice: 1
    ui_slice_timeout_seconds: 30
    rpa_timeout_seconds: 180
    state_machine_enabled: true
    parse_after_release_ui: true
    resume_from_progress: true
    min_article_ui_window_seconds: 20
    low_peak_windows:
      - "08:30-09:30"
  ui_resource:
    mode: exclusive
    group_priority: true
    max_core_group_block_seconds: 10
    lock_acquire_timeout_seconds: 5
    lock_lease_seconds: 120
    lock_heartbeat_interval_seconds: 10
    stale_lock_recover_seconds: 180
    metrics_enabled: true

mysql:
  host: 127.0.0.1
  port: 3307
  database: weinsight_dev
  username: weinsight
  password: ${WEINSIGHT_MYSQL_PASSWORD}
```

- [ ] **Step 4: Implement config loader**

Create `app/core/config.py`:

```python
from __future__ import annotations

import os
import re
from dataclasses import dataclass
from pathlib import Path
from typing import Any

import yaml


ENV_PATTERN = re.compile(r"\$\{([A-Z0-9_]+)\}")


@dataclass(frozen=True)
class AppConfig:
    env: str
    timezone: str
    log_level: str


@dataclass(frozen=True)
class WechatConfig:
    pc_version: str
    window_title: str
    fixed_window: bool
    check_login_interval_seconds: int


@dataclass(frozen=True)
class MysqlConfig:
    host: str
    port: int
    database: str
    username: str
    password: str


@dataclass(frozen=True)
class GroupPipelineConfig:
    enabled: bool
    core_group_limit: int
    poll_interval_seconds: int
    max_group_per_round: int
    rpa_timeout_seconds: int
    target_round_seconds: int


@dataclass(frozen=True)
class ArticlePipelineConfig:
    enabled: bool
    account_limit: int
    crawl_time: str
    max_articles_per_account: int
    max_accounts_per_ui_slice: int
    ui_slice_timeout_seconds: int
    rpa_timeout_seconds: int
    state_machine_enabled: bool
    parse_after_release_ui: bool
    resume_from_progress: bool
    min_article_ui_window_seconds: int
    low_peak_windows: tuple[str, ...]


@dataclass(frozen=True)
class UiResourceConfig:
    mode: str
    group_priority: bool
    max_core_group_block_seconds: int
    lock_acquire_timeout_seconds: int
    lock_lease_seconds: int
    lock_heartbeat_interval_seconds: int
    stale_lock_recover_seconds: int
    metrics_enabled: bool


@dataclass(frozen=True)
class PipelineConfig:
    group: GroupPipelineConfig
    article: ArticlePipelineConfig
    ui_resource: UiResourceConfig


@dataclass(frozen=True)
class Config:
    app: AppConfig
    wechat: WechatConfig
    pipelines: PipelineConfig
    mysql: MysqlConfig


def _expand_env(value: Any) -> Any:
    if isinstance(value, str):
        def replace(match: re.Match[str]) -> str:
            name = match.group(1)
            if name not in os.environ:
                raise RuntimeError(f"Missing environment variable: {name}")
            return os.environ[name]

        return ENV_PATTERN.sub(replace, value)
    if isinstance(value, dict):
        return {key: _expand_env(item) for key, item in value.items()}
    if isinstance(value, list):
        return [_expand_env(item) for item in value]
    return value


def load_config(path: Path) -> Config:
    raw = yaml.safe_load(path.read_text(encoding="utf-8"))
    data = _expand_env(raw)

    return Config(
        app=AppConfig(**data["app"]),
        wechat=WechatConfig(**data["wechat"]),
        pipelines=PipelineConfig(
            group=GroupPipelineConfig(**data["pipelines"]["group"]),
            article=ArticlePipelineConfig(
                **{
                    **data["pipelines"]["article"],
                    "low_peak_windows": tuple(data["pipelines"]["article"]["low_peak_windows"]),
                }
            ),
            ui_resource=UiResourceConfig(**data["pipelines"]["ui_resource"]),
        ),
        mysql=MysqlConfig(**data["mysql"]),
    )
```

- [ ] **Step 5: Run config tests**

Run:

```powershell
pytest tests/test_config.py -q
```

Expected: `2 passed`.

---

### Task 3: Domain Utilities

**Files:**
- Create: `app/domain/hashes.py`
- Create: `app/domain/desensitize.py`
- Test: `tests/test_hashes.py`
- Test: `tests/test_desensitize.py`

- [ ] **Step 1: Write failing hash tests**

Create `tests/test_hashes.py`:

```python
from __future__ import annotations

from app.domain.hashes import article_hash, group_msg_hash


def test_group_msg_hash_is_stable() -> None:
    first = group_msg_hash(
        group_name="核心交易群A",
        sender_name="张三",
        msg_time_display="2026-07-02 08:31",
        msg_content="求购鸡蛋 30 箱 北京",
        msg_type="text",
    )
    second = group_msg_hash(
        group_name="核心交易群A",
        sender_name="张三",
        msg_time_display="2026-07-02 08:31",
        msg_content="求购鸡蛋 30 箱 北京",
        msg_type="text",
    )

    assert first == second
    assert len(first) == 64


def test_article_hash_is_stable() -> None:
    value = article_hash(
        account_name="行业观察",
        title="鸡蛋市场日报",
        publish_time="2026-07-02 08:00:00",
        url="https://mp.weixin.qq.com/s/example",
    )

    assert len(value) == 64
```

- [ ] **Step 2: Write failing desensitize tests**

Create `tests/test_desensitize.py`:

```python
from __future__ import annotations

from app.domain.desensitize import mask_phone, mask_wechat_id


def test_mask_phone() -> None:
    assert mask_phone("联系人 13812345678") == "联系人 138****5678"


def test_mask_wechat_id() -> None:
    assert mask_wechat_id("微信 wxid_abc123 可联系") == "微信 [微信号已脱敏] 可联系"
```

- [ ] **Step 3: Run tests to verify failure**

Run:

```powershell
pytest tests/test_hashes.py tests/test_desensitize.py -q
```

Expected: FAIL because modules do not exist.

- [ ] **Step 4: Implement hash utilities**

Create `app/domain/hashes.py`:

```python
from __future__ import annotations

import hashlib


def _sha256_join(parts: list[str | None]) -> str:
    normalized = "\u241f".join("" if part is None else str(part).strip() for part in parts)
    return hashlib.sha256(normalized.encode("utf-8")).hexdigest()


def group_msg_hash(
    *,
    group_name: str,
    sender_name: str | None,
    msg_time_display: str | None,
    msg_content: str | None,
    msg_type: str | None,
) -> str:
    return _sha256_join([group_name, sender_name, msg_time_display, msg_content, msg_type])


def article_hash(
    *,
    account_name: str,
    title: str | None,
    publish_time: str | None,
    url: str | None,
) -> str:
    return _sha256_join([account_name, title, publish_time, url])
```

- [ ] **Step 5: Implement desensitize utilities**

Create `app/domain/desensitize.py`:

```python
from __future__ import annotations

import re


PHONE_PATTERN = re.compile(r"(?<!\d)(1[3-9]\d)(\d{4})(\d{4})(?!\d)")
WECHAT_ID_PATTERN = re.compile(r"(?<![A-Za-z0-9_])(?:wxid_[A-Za-z0-9_-]+|[A-Za-z][A-Za-z0-9_-]{5,19})(?![A-Za-z0-9_])")


def mask_phone(text: str) -> str:
    return PHONE_PATTERN.sub(r"\1****\3", text)


def mask_wechat_id(text: str) -> str:
    return WECHAT_ID_PATTERN.sub("[微信号已脱敏]", text)
```

- [ ] **Step 6: Run domain tests**

Run:

```powershell
pytest tests/test_hashes.py tests/test_desensitize.py -q
```

Expected: `4 passed`.

---

### Task 4: SQL Schema And DB Connection

**Files:**
- Create: `sql/init.sql`
- Create: `app/storage/db.py`
- Create: `app/storage/schema.py`
- Test: `tests/test_pipeline_isolation.py`

- [ ] **Step 1: Write failing isolation schema test**

Create `tests/test_pipeline_isolation.py`:

```python
from __future__ import annotations

from pathlib import Path


def test_init_sql_has_separate_group_and_article_tables() -> None:
    sql = Path("sql/init.sql").read_text(encoding="utf-8")

    assert "CREATE TABLE IF NOT EXISTS wechat_group_process_task" in sql
    assert "CREATE TABLE IF NOT EXISTS wechat_article_process_task" in sql
    assert "CREATE TABLE IF NOT EXISTS wechat_group_collect_log" in sql
    assert "CREATE TABLE IF NOT EXISTS wechat_article_collect_log" in sql
    assert "CREATE TABLE IF NOT EXISTS wechat_article_collect_progress" in sql
    assert "CREATE TABLE IF NOT EXISTS wechat_ui_lock" in sql
```

- [ ] **Step 2: Run schema test to verify failure**

Run:

```powershell
pytest tests/test_pipeline_isolation.py -q
```

Expected: FAIL because `sql/init.sql` does not exist.

- [ ] **Step 3: Create SQL init script**

Create `sql/init.sql` with these tables:

```sql
CREATE TABLE IF NOT EXISTS wechat_group_process_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_type VARCHAR(50) NOT NULL COMMENT 'clean_group_msg/analyze_group_msg/group_daily_report',
    ref_type VARCHAR(50) NOT NULL COMMENT 'msg/date',
    ref_id VARCHAR(100) NOT NULL COMMENT '消息hash或日期',
    status VARCHAR(50) NOT NULL DEFAULT 'pending' COMMENT 'pending/running/success/failed/skipped',
    retry_count INT NOT NULL DEFAULT 0 COMMENT '重试次数',
    next_run_time DATETIME NULL COMMENT '下次执行时间',
    error_msg TEXT NULL COMMENT '失败原因',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_group_task_ref (task_type, ref_type, ref_id),
    KEY idx_group_status_next_run (status, next_run_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS wechat_article_process_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_type VARCHAR(50) NOT NULL COMMENT 'clean_article/analyze_article/article_daily_report',
    ref_type VARCHAR(50) NOT NULL COMMENT 'article/date',
    ref_id VARCHAR(100) NOT NULL COMMENT '文章hash或日期',
    status VARCHAR(50) NOT NULL DEFAULT 'pending' COMMENT 'pending/running/success/failed/skipped',
    retry_count INT NOT NULL DEFAULT 0 COMMENT '重试次数',
    next_run_time DATETIME NULL COMMENT '下次执行时间',
    error_msg TEXT NULL COMMENT '失败原因',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_article_task_ref (task_type, ref_type, ref_id),
    KEY idx_article_status_next_run (status, next_run_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS wechat_group_collect_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    batch_id VARCHAR(64) NOT NULL,
    source_name VARCHAR(200) NOT NULL,
    start_time DATETIME NOT NULL,
    end_time DATETIME NULL,
    scan_pages INT DEFAULT 0,
    read_count INT DEFAULT 0,
    insert_count INT DEFAULT 0,
    duplicate_count INT DEFAULT 0,
    status VARCHAR(50) NOT NULL,
    error_code VARCHAR(100) NULL,
    error_msg TEXT NULL,
    screenshot_path VARCHAR(500) NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_group_log_source_time (source_name, start_time),
    KEY idx_group_log_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS wechat_article_collect_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    batch_id VARCHAR(64) NOT NULL,
    account_name VARCHAR(200) NOT NULL,
    start_time DATETIME NOT NULL,
    end_time DATETIME NULL,
    link_count INT DEFAULT 0,
    insert_count INT DEFAULT 0,
    status VARCHAR(50) NOT NULL,
    stage VARCHAR(50) NULL,
    error_code VARCHAR(100) NULL,
    error_msg TEXT NULL,
    screenshot_path VARCHAR(500) NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_article_log_account_time (account_name, start_time),
    KEY idx_article_log_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS wechat_article_collect_progress (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    crawl_date DATE NOT NULL COMMENT '采集日期',
    account_name VARCHAR(200) NOT NULL COMMENT '公众号或订阅号名称',
    stage VARCHAR(50) NOT NULL COMMENT 'open_account/list_articles/copy_links/save_links/parse_article/done',
    last_article_url TEXT NULL COMMENT '最近已处理文章链接',
    status VARCHAR(50) NOT NULL DEFAULT 'pending' COMMENT 'pending/running/interrupted/success/failed/skipped',
    retry_count INT NOT NULL DEFAULT 0 COMMENT '重试次数',
    last_error_code VARCHAR(100) NULL COMMENT '最近失败错误码',
    last_error_msg TEXT NULL COMMENT '最近失败原因',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_article_progress (crawl_date, account_name),
    KEY idx_article_progress_status (status, stage)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS wechat_ui_lock (
    lock_name VARCHAR(100) PRIMARY KEY,
    owner_pipeline VARCHAR(50) NOT NULL,
    owner_task_id VARCHAR(100) NOT NULL,
    acquire_time DATETIME NOT NULL,
    heartbeat_time DATETIME NOT NULL,
    expire_time DATETIME NOT NULL,
    lease_seconds INT NOT NULL,
    wait_seconds DECIMAL(10,3) DEFAULT 0,
    stale_recovered_by VARCHAR(100) NULL,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

- [ ] **Step 4: Create DB helper**

Create `app/storage/db.py`:

```python
from __future__ import annotations

from sqlalchemy import create_engine
from sqlalchemy.engine import Engine

from app.core.config import MysqlConfig


def create_mysql_engine(config: MysqlConfig) -> Engine:
    url = (
        f"mysql+pymysql://{config.username}:{config.password}"
        f"@{config.host}:{config.port}/{config.database}?charset=utf8mb4"
    )
    return create_engine(url, pool_pre_ping=True, pool_recycle=1800)
```

- [ ] **Step 5: Create schema placeholder module for imports**

Create `app/storage/schema.py`:

```python
from __future__ import annotations

from pathlib import Path


def read_init_sql() -> str:
    return Path("sql/init.sql").read_text(encoding="utf-8")
```

- [ ] **Step 6: Run schema test**

Run:

```powershell
pytest tests/test_pipeline_isolation.py -q
```

Expected: `1 passed`.

- [ ] **Step 7: Initialize development database**

Run:

```powershell
docker exec -i pulsebrief-mysql mysql -uweinsight -pweinsight_dev weinsight_dev < sql/init.sql
```

Expected: command exits with code `0`.

---

### Task 5: UI Lock Lease Repository

**Files:**
- Create: `app/storage/lock_repo.py`
- Test: `tests/test_ui_lock.py`

- [ ] **Step 1: Write failing UI lock tests**

Create `tests/test_ui_lock.py`:

```python
from __future__ import annotations

from datetime import datetime, timedelta

from app.storage.lock_repo import InMemoryUiLockRepo


def test_lock_acquire_and_release() -> None:
    repo = InMemoryUiLockRepo()
    now = datetime(2026, 7, 2, 8, 0, 0)

    acquired = repo.acquire(
        lock_name="wechat_ui",
        owner_pipeline="group",
        owner_task_id="group-1",
        now=now,
        lease_seconds=120,
    )

    assert acquired is True
    assert repo.current_owner("wechat_ui") == "group"

    repo.release("wechat_ui", owner_pipeline="group", owner_task_id="group-1")
    assert repo.current_owner("wechat_ui") is None


def test_stale_lock_can_be_recovered() -> None:
    repo = InMemoryUiLockRepo()
    now = datetime(2026, 7, 2, 8, 0, 0)

    assert repo.acquire("wechat_ui", "article", "article-1", now, 60) is True
    recovered = repo.recover_stale(
        lock_name="wechat_ui",
        recovered_by="group-scheduler",
        now=now + timedelta(seconds=181),
        stale_after_seconds=180,
    )

    assert recovered is True
    assert repo.current_owner("wechat_ui") is None
    assert repo.stale_lock_recovered_count == 1
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```powershell
pytest tests/test_ui_lock.py -q
```

Expected: FAIL because `lock_repo.py` does not exist.

- [ ] **Step 3: Implement in-memory lock repo**

Create `app/storage/lock_repo.py`:

```python
from __future__ import annotations

from dataclasses import dataclass
from datetime import datetime, timedelta


@dataclass
class UiLock:
    lock_name: str
    owner_pipeline: str
    owner_task_id: str
    acquire_time: datetime
    heartbeat_time: datetime
    expire_time: datetime
    lease_seconds: int


class InMemoryUiLockRepo:
    def __init__(self) -> None:
        self._locks: dict[str, UiLock] = {}
        self.stale_lock_recovered_count = 0

    def acquire(
        self,
        lock_name: str,
        owner_pipeline: str,
        owner_task_id: str,
        now: datetime,
        lease_seconds: int,
    ) -> bool:
        existing = self._locks.get(lock_name)
        if existing is not None and existing.expire_time > now:
            return False

        self._locks[lock_name] = UiLock(
            lock_name=lock_name,
            owner_pipeline=owner_pipeline,
            owner_task_id=owner_task_id,
            acquire_time=now,
            heartbeat_time=now,
            expire_time=now + timedelta(seconds=lease_seconds),
            lease_seconds=lease_seconds,
        )
        return True

    def heartbeat(
        self,
        lock_name: str,
        owner_pipeline: str,
        owner_task_id: str,
        now: datetime,
    ) -> bool:
        lock = self._locks.get(lock_name)
        if lock is None:
            return False
        if lock.owner_pipeline != owner_pipeline or lock.owner_task_id != owner_task_id:
            return False
        lock.heartbeat_time = now
        lock.expire_time = now + timedelta(seconds=lock.lease_seconds)
        return True

    def release(self, lock_name: str, owner_pipeline: str, owner_task_id: str) -> bool:
        lock = self._locks.get(lock_name)
        if lock is None:
            return False
        if lock.owner_pipeline != owner_pipeline or lock.owner_task_id != owner_task_id:
            return False
        del self._locks[lock_name]
        return True

    def recover_stale(
        self,
        lock_name: str,
        recovered_by: str,
        now: datetime,
        stale_after_seconds: int,
    ) -> bool:
        lock = self._locks.get(lock_name)
        if lock is None:
            return False
        stale_at = lock.heartbeat_time + timedelta(seconds=stale_after_seconds)
        if stale_at > now:
            return False
        del self._locks[lock_name]
        self.stale_lock_recovered_count += 1
        return True

    def current_owner(self, lock_name: str) -> str | None:
        lock = self._locks.get(lock_name)
        return None if lock is None else lock.owner_pipeline
```

- [ ] **Step 4: Run UI lock tests**

Run:

```powershell
pytest tests/test_ui_lock.py -q
```

Expected: `2 passed`.

---

### Task 6: RPA Interfaces And Fake Clients

**Files:**
- Create: `app/rpa/interfaces.py`
- Create: `app/rpa/fake_clients.py`

- [ ] **Step 1: Create RPA interfaces**

Create `app/rpa/interfaces.py`:

```python
from __future__ import annotations

from dataclasses import dataclass
from typing import Protocol


@dataclass(frozen=True)
class VisibleMessage:
    group_name: str
    sender_name: str
    msg_time_display: str
    msg_content: str
    msg_type: str = "text"


class WechatDesktopClient(Protocol):
    def get_client_version(self) -> str:
        ...

    def check_login_status(self) -> bool:
        ...

    def save_screenshot(self, path: str) -> str:
        ...


class WechatGroupRpaClient(Protocol):
    def open_group(self, group_name: str) -> None:
        ...

    def read_visible_messages(self) -> list[VisibleMessage]:
        ...

    def scroll_up_messages(self, pages: int = 1) -> None:
        ...


class WechatArticleRpaClient(Protocol):
    def open_public_account(self, account_name: str) -> None:
        ...

    def copy_latest_article_links(self, max_articles: int) -> list[str]:
        ...
```

- [ ] **Step 2: Create fake clients**

Create `app/rpa/fake_clients.py`:

```python
from __future__ import annotations

from app.rpa.interfaces import VisibleMessage


class FakeDesktopClient:
    def __init__(self, version: str = "4.1.10.31", logged_in: bool = True) -> None:
        self.version = version
        self.logged_in = logged_in
        self.screenshots: list[str] = []

    def get_client_version(self) -> str:
        return self.version

    def check_login_status(self) -> bool:
        return self.logged_in

    def save_screenshot(self, path: str) -> str:
        self.screenshots.append(path)
        return path


class FakeGroupRpaClient:
    def __init__(self, messages: list[VisibleMessage]) -> None:
        self.messages = messages
        self.opened_groups: list[str] = []
        self.scroll_count = 0

    def open_group(self, group_name: str) -> None:
        self.opened_groups.append(group_name)

    def read_visible_messages(self) -> list[VisibleMessage]:
        return list(self.messages)

    def scroll_up_messages(self, pages: int = 1) -> None:
        self.scroll_count += pages


class FakeArticleRpaClient:
    def __init__(self, links_by_account: dict[str, list[str]]) -> None:
        self.links_by_account = links_by_account
        self.opened_accounts: list[str] = []

    def open_public_account(self, account_name: str) -> None:
        self.opened_accounts.append(account_name)

    def copy_latest_article_links(self, max_articles: int) -> list[str]:
        if not self.opened_accounts:
            return []
        account = self.opened_accounts[-1]
        return self.links_by_account.get(account, [])[:max_articles]
```

- [ ] **Step 3: Run existing tests**

Run:

```powershell
pytest -q
```

Expected: all existing tests pass.

---

### Task 7: Article State Machine

**Files:**
- Create: `app/pipelines/article_pipeline.py`
- Test: `tests/test_article_state_machine.py`

- [ ] **Step 1: Write failing article state machine tests**

Create `tests/test_article_state_machine.py`:

```python
from __future__ import annotations

from datetime import datetime, timedelta

from app.pipelines.article_pipeline import (
    ArticleProgress,
    ArticleStage,
    ArticleUiDecision,
    should_defer_article_ui,
)


def test_article_ui_defers_when_core_group_due_soon() -> None:
    now = datetime(2026, 7, 2, 8, 30, 0)
    next_core_group_due = now + timedelta(seconds=10)

    decision = should_defer_article_ui(
        now=now,
        next_core_group_due=next_core_group_due,
        min_article_ui_window_seconds=20,
    )

    assert decision == ArticleUiDecision.DEFER


def test_article_progress_can_resume_after_interruption() -> None:
    progress = ArticleProgress(
        crawl_date="2026-07-02",
        account_name="行业观察",
        stage=ArticleStage.COPY_LINKS,
        status="interrupted",
        last_article_url="https://mp.weixin.qq.com/s/1",
        retry_count=1,
    )

    resumed = progress.mark_running()

    assert resumed.stage == ArticleStage.COPY_LINKS
    assert resumed.status == "running"
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```powershell
pytest tests/test_article_state_machine.py -q
```

Expected: FAIL because `article_pipeline.py` does not exist.

- [ ] **Step 3: Implement article state helpers**

Create `app/pipelines/article_pipeline.py`:

```python
from __future__ import annotations

from dataclasses import dataclass, replace
from datetime import datetime
from enum import Enum


class ArticleStage(str, Enum):
    OPEN_ACCOUNT = "open_account"
    LIST_ARTICLES = "list_articles"
    COPY_LINKS = "copy_links"
    SAVE_LINKS = "save_links"
    RELEASE_UI = "release_ui"
    PARSE_ARTICLE_IN_BROWSER = "parse_article_in_browser"
    WRITE_ARTICLE_RAW = "write_article_raw"
    DONE = "done"


class ArticleUiDecision(str, Enum):
    RUN = "run"
    DEFER = "defer"


@dataclass(frozen=True)
class ArticleProgress:
    crawl_date: str
    account_name: str
    stage: ArticleStage
    status: str
    last_article_url: str | None = None
    retry_count: int = 0

    def mark_running(self) -> "ArticleProgress":
        return replace(self, status="running")

    def mark_interrupted(self) -> "ArticleProgress":
        return replace(self, status="interrupted", retry_count=self.retry_count + 1)


def should_defer_article_ui(
    *,
    now: datetime,
    next_core_group_due: datetime,
    min_article_ui_window_seconds: int,
) -> ArticleUiDecision:
    remaining = (next_core_group_due - now).total_seconds()
    if remaining < min_article_ui_window_seconds:
        return ArticleUiDecision.DEFER
    return ArticleUiDecision.RUN
```

- [ ] **Step 4: Run article state machine tests**

Run:

```powershell
pytest tests/test_article_state_machine.py -q
```

Expected: `2 passed`.

---

### Task 8: Pipeline Isolation Skeleton

**Files:**
- Create: `app/pipelines/group_pipeline.py`
- Modify: `app/pipelines/article_pipeline.py`
- Test: `tests/test_pipeline_isolation.py`

- [ ] **Step 1: Extend isolation tests**

Append to `tests/test_pipeline_isolation.py`:

```python
from app.pipelines.article_pipeline import ArticlePipelineState
from app.pipelines.group_pipeline import GroupPipelineState


def test_pipeline_state_is_separate() -> None:
    group_state = GroupPipelineState(pending_tasks=2, failed_tasks=0)
    article_state = ArticlePipelineState(pending_tasks=5, failed_tasks=1)

    article_state = article_state.mark_failed()

    assert group_state.failed_tasks == 0
    assert article_state.failed_tasks == 2
```

- [ ] **Step 2: Run test to verify failure**

Run:

```powershell
pytest tests/test_pipeline_isolation.py -q
```

Expected: FAIL because `GroupPipelineState` and `ArticlePipelineState` are missing.

- [ ] **Step 3: Implement group state**

Create `app/pipelines/group_pipeline.py`:

```python
from __future__ import annotations

from dataclasses import dataclass, replace


@dataclass(frozen=True)
class GroupPipelineState:
    pending_tasks: int
    failed_tasks: int

    def mark_failed(self) -> "GroupPipelineState":
        return replace(self, failed_tasks=self.failed_tasks + 1)
```

- [ ] **Step 4: Add article state**

Append to `app/pipelines/article_pipeline.py`:

```python

@dataclass(frozen=True)
class ArticlePipelineState:
    pending_tasks: int
    failed_tasks: int

    def mark_failed(self) -> "ArticlePipelineState":
        return replace(self, failed_tasks=self.failed_tasks + 1)
```

- [ ] **Step 5: Run isolation tests**

Run:

```powershell
pytest tests/test_pipeline_isolation.py -q
```

Expected: all tests in `tests/test_pipeline_isolation.py` pass.

---

### Task 9: Command Entrypoints

**Files:**
- Create: `app/main.py`

- [ ] **Step 1: Create CLI entrypoint**

Create `app/main.py`:

```python
from __future__ import annotations

import argparse
from pathlib import Path

from app.core.config import load_config
from app.storage.schema import read_init_sql


def main() -> int:
    parser = argparse.ArgumentParser(prog="weinsight")
    parser.add_argument("command", choices=["check-config", "print-init-sql"])
    parser.add_argument("--config", default="config/config.dev.yaml")
    args = parser.parse_args()

    if args.command == "check-config":
        config = load_config(Path(args.config))
        print(f"env={config.app.env}")
        print(f"wechat_pc_version={config.wechat.pc_version}")
        print(f"group_limit={config.pipelines.group.core_group_limit}")
        print(f"article_limit={config.pipelines.article.account_limit}")
        return 0

    if args.command == "print-init-sql":
        print(read_init_sql())
        return 0

    return 2


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: Verify config command**

Run:

```powershell
python -m app.main check-config --config config/config.dev.yaml
```

Expected output includes:

```text
env=dev
wechat_pc_version=4.1.10.31
group_limit=5
article_limit=20
```

- [ ] **Step 3: Verify SQL command**

Run:

```powershell
python -m app.main print-init-sql
```

Expected output includes:

```text
CREATE TABLE IF NOT EXISTS wechat_group_process_task
CREATE TABLE IF NOT EXISTS wechat_article_process_task
CREATE TABLE IF NOT EXISTS wechat_ui_lock
```

---

### Task 10: Full Verification

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Update README verification section**

Append to `README.md`:

```markdown

## 第一阶段验证

```powershell
python -m pip install -r requirements.txt
pytest -q
python -m app.main check-config --config config/config.dev.yaml
python -m app.main print-init-sql
docker exec -i pulsebrief-mysql mysql -uweinsight -pweinsight_dev weinsight_dev < sql/init.sql
```

验收重点：

- `wechat_group_process_task` 和 `wechat_article_process_task` 分表
- `wechat_ui_lock` 支持租约和过期恢复
- 公众号/订阅号链路可按进度恢复
- Playwright 正文解析不持有微信 UI 锁
```

- [ ] **Step 2: Run full test suite**

Run:

```powershell
pytest -q
```

Expected: all tests pass.

- [ ] **Step 3: Run config command**

Run:

```powershell
python -m app.main check-config --config config/config.dev.yaml
```

Expected: command exits with code `0`.

- [ ] **Step 4: Initialize MySQL schema**

Run:

```powershell
docker exec -i pulsebrief-mysql mysql -uweinsight -pweinsight_dev weinsight_dev < sql/init.sql
```

Expected: command exits with code `0`.

- [ ] **Step 5: Confirm expected tables exist**

Run:

```powershell
docker exec pulsebrief-mysql mysql -uweinsight -pweinsight_dev weinsight_dev -e "SHOW TABLES LIKE 'wechat_%';"
```

Expected output includes:

```text
wechat_article_collect_log
wechat_article_collect_progress
wechat_article_process_task
wechat_group_collect_log
wechat_group_process_task
wechat_ui_lock
```

---

## 自检结果

设计要求覆盖：

- 双链路隔离：Task 4, Task 8
- 开发库配置：Task 2, Task 9
- UI 锁租约和僵死锁恢复：Task 5
- 文章链路按账号分片：Task 7
- 文章链路中断恢复：Task 4, Task 7
- 先拿链接后解析：Task 7, Task 10
- 测试与回归：Task 1 到 Task 10 均包含验证步骤

实施顺序：

1. 先完成 Task 1 到 Task 5，建立可测试基础设施。
2. 再完成 Task 6 到 Task 8，建立 fake RPA 和双链路骨架。
3. 最后完成 Task 9 到 Task 10，形成可运行命令和数据库初始化闭环。
