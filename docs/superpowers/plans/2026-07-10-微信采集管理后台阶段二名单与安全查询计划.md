# WeInsight 管理后台阶段二名单与安全查询 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成采集控制面数据表、微信群/公众号名单安全管理，以及仅查询脱敏 clean/analysis 和日报的只读页面。

**Architecture:** 先创建任务控制表以便数据库外键保护名单删除，再扩展现有配置 Repository。安全结果使用独立 Repo 显式选择允许字段，Web 路由只调用 Service，不复用 raw Repo。

**Tech Stack:** Python 3.12、FastAPI、Jinja2、HTMX 2.0.4、ECharts 5.6.0、SQLAlchemy、PyMySQL、MySQL、pytest、FastAPI TestClient。

## Global Constraints

- 必须先完成阶段一计划。
- Web 数据库路径不得查询 `wechat_group_msg_raw`。
- 文章响应不得包含 `article_url`、正文、HTML 或运行时临时正文。
- 名单删除只删除配置行，不删除历史 raw、clean、analysis、日志和日报。
- 被任意任务目标引用的名单不能删除；被 scheduled、active、stop_requested 任务引用的名单不能停用。
- 名称产生历史数据或被任务引用后禁止改名。
- 群/公众号配置继续使用独立表和独立 Service 方法。
- 所有列表使用服务端分页，`page_size` 范围为 1 到 100。
- 故障截图内容不通过 Web 返回；本阶段只保留后续展示路径所需字段。
- 本阶段不启动采集 Worker 和后处理 Worker。

---

### Task 1: 创建采集控制面数据表

**Files:**
- Create: `sql/migrations/20260710_002_create_collection_control.sql`
- Modify: `sql/init.sql`
- Create: `tests/test_collection_control_sql_schema.py`

**Interfaces:**
- Consumes: 现有 `wechat_group_config.id`、`wechat_public_account_config.id`、管理员表。
- Produces: job、target、run、target_run、event、worker heartbeat、微信健康表。

- [ ] **Step 1: 写 schema 失败测试**

创建 `tests/test_collection_control_sql_schema.py`：

```python
from pathlib import Path


TABLES = (
    "wechat_collection_job",
    "wechat_collection_job_target",
    "wechat_collection_job_run",
    "wechat_collection_job_target_run",
    "wechat_collection_job_event",
    "wechat_worker_heartbeat",
    "wechat_client_health_check",
)


def test_collection_control_schema_is_additive_and_restricts_source_delete() -> None:
    migration = Path(
        "sql/migrations/20260710_002_create_collection_control.sql"
    ).read_text(encoding="utf-8")
    init_sql = Path("sql/init.sql").read_text(encoding="utf-8")
    for table in TABLES:
        assert f"CREATE TABLE IF NOT EXISTS {table}" in migration
        assert f"CREATE TABLE IF NOT EXISTS {table}" in init_sql
    assert "fk_job_target_group" in migration
    assert "fk_job_target_article" in migration
    assert migration.upper().count("ON DELETE RESTRICT") >= 2
    assert "uk_job_schedule" in migration
    assert "DROP TABLE" not in migration.upper()
    assert "TRUNCATE TABLE" not in migration.upper()
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
pytest -q tests/test_collection_control_sql_schema.py
```

Expected: FAIL，迁移不存在。

- [ ] **Step 3: 编写任务与运行表 DDL**

任务和目标 DDL 使用：

```sql
CREATE TABLE IF NOT EXISTS wechat_collection_job (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '采集任务主键',
    job_name VARCHAR(200) NOT NULL COMMENT '采集任务名称',
    pipeline_type VARCHAR(20) NOT NULL COMMENT 'group/article',
    effective_start_at DATETIME NOT NULL COMMENT '任务整体开始时间',
    effective_end_at DATETIME NOT NULL COMMENT '任务整体结束时间',
    daily_window_start TIME NOT NULL COMMENT '每日运行窗口开始',
    daily_window_end TIME NOT NULL COMMENT '每日运行窗口结束',
    interval_seconds INT NOT NULL COMMENT '任务执行间隔秒数',
    status VARCHAR(30) NOT NULL COMMENT 'scheduled/active/stop_requested/stopped/completed/deleted',
    next_run_at DATETIME NULL COMMENT '下次计划运行时间',
    stop_requested_at DATETIME NULL COMMENT '停止请求时间',
    stop_requested_by VARCHAR(100) NULL COMMENT '停止请求管理员',
    deleted_at DATETIME NULL COMMENT '软删除时间',
    deleted_by VARCHAR(100) NULL COMMENT '软删除管理员',
    version INT NOT NULL DEFAULT 1 COMMENT '乐观锁版本',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY idx_collection_job_due (status, next_run_at),
    KEY idx_collection_job_window (effective_start_at, effective_end_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS wechat_collection_job_target (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '任务目标主键',
    job_id BIGINT NOT NULL COMMENT '采集任务主键',
    group_config_id BIGINT NULL COMMENT '微信群配置主键',
    article_config_id BIGINT NULL COMMENT '公众号配置主键',
    target_name_snapshot VARCHAR(200) NOT NULL COMMENT '目标名称快照',
    priority_snapshot INT NOT NULL COMMENT '优先级快照',
    config_snapshot_json TEXT NOT NULL COMMENT '配置快照JSON',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_job_group_target (job_id, group_config_id),
    UNIQUE KEY uk_job_article_target (job_id, article_config_id),
    CONSTRAINT ck_job_target_exactly_one CHECK (
        (group_config_id IS NOT NULL AND article_config_id IS NULL)
        OR (group_config_id IS NULL AND article_config_id IS NOT NULL)
    ),
    CONSTRAINT fk_job_target_job FOREIGN KEY (job_id)
        REFERENCES wechat_collection_job(id) ON DELETE RESTRICT,
    CONSTRAINT fk_job_target_group FOREIGN KEY (group_config_id)
        REFERENCES wechat_group_config(id) ON DELETE RESTRICT,
    CONSTRAINT fk_job_target_article FOREIGN KEY (article_config_id)
        REFERENCES wechat_public_account_config(id) ON DELETE RESTRICT
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

运行表必须包含设计文档列，并在 `wechat_collection_job_run` 建立：

```sql
UNIQUE KEY uk_job_schedule (job_id, scheduled_at)
```

事件表按 `(run_id, id)`、`(job_id, create_time)` 建索引；健康表按 `checked_at` 和 `status` 建索引；heartbeat 以 `worker_id` 为主键。

- [ ] **Step 4: 在 init.sql 同步完整 DDL 并验证**

Run:

```powershell
pytest -q tests/test_collection_control_sql_schema.py tests/test_sql_migrations.py
```

Expected: PASS。

- [ ] **Step 5: 提交控制面 schema**

```powershell
git add sql/init.sql sql/migrations/20260710_002_create_collection_control.sql tests/test_collection_control_sql_schema.py
git commit -m "feat: add collection control schema"
```

### Task 2: 扩展配置 Repo 并实现名单安全规则

**Files:**
- Modify: `app/storage/group_repo.py`
- Modify: `app/storage/article_config_repo.py`
- Create: `app/storage/source_reference_repo.py`
- Create: `app/services/source_management_service.py`
- Modify: `tests/test_group_config_repo.py`
- Modify: `tests/test_article_account_config_repo.py`
- Create: `tests/test_source_management_service.py`

**Interfaces:**
- Consumes: 两套配置表、`wechat_collection_job_target` 和业务历史表。
- Produces: `SourceManagementService.list/create/update/set_enabled/delete`，ID 稳定且删除受引用保护。

- [ ] **Step 1: 写名单规则失败测试**

创建 `tests/test_source_management_service.py`：

```python
def test_active_reference_blocks_disable(service, refs) -> None:
    refs.active_job_names = ["核心群晨间采集"]
    with pytest.raises(SourceInUseError) as exc:
        service.set_group_enabled(source_id=7, enabled=False)
    assert exc.value.job_names == ("核心群晨间采集",)


def test_any_reference_blocks_delete(service, refs) -> None:
    refs.all_job_names = ["已完成任务"]
    with pytest.raises(SourceInUseError):
        service.delete_group(source_id=7)


def test_unreferenced_disabled_source_can_be_deleted(service, group_repo) -> None:
    group_repo.record.enabled = False
    service.delete_group(source_id=7)
    assert group_repo.deleted_ids == [7]


def test_source_with_history_cannot_be_renamed(service, group_repo, refs) -> None:
    refs.has_group_history_result = True
    with pytest.raises(SourceRenameBlockedError):
        service.update_group(
            source_id=7,
            command=GroupSourceCommand(
                group_name="新群名",
                is_core_group=True,
                priority=1,
                poll_interval_seconds=30,
                backtrack_pages=1,
                extra_backtrack_pages=3,
                remark=None,
            ),
        )
```

测试中的 `GroupSourceCommand` 必须传入完整字段：`group_name`、`is_core_group`、`priority`、`poll_interval_seconds`、`backtrack_pages`、`extra_backtrack_pages`、`remark`。

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
pytest -q tests/test_source_management_service.py
```

Expected: FAIL，Service 不存在。

- [ ] **Step 3: 为现有 Record 增加稳定 ID**

在两个 dataclass 最后增加默认字段，避免破坏现有构造调用：

```python
@dataclass(frozen=True)
class GroupConfigRecord:
    group_name: str
    priority: int
    poll_interval_seconds: int
    enabled: bool = True
    is_core_group: bool = False
    backtrack_pages: int = 10
    extra_backtrack_pages: int = 30
    remark: str | None = None
    id: int | None = None
```

`ArticleAccountConfigRecord` 同样在末尾增加 `id: int | None = None`。所有 list/get SQL 显式 SELECT `id`。

- [ ] **Step 4: 增加 Repo 精确方法**

`MysqlGroupConfigRepo` 增加：

```python
get_group(source_id: int) -> GroupConfigRecord | None
set_group_enabled(source_id: int, enabled: bool) -> int
delete_group(source_id: int) -> int
```

`MysqlArticleAccountConfigRepo` 增加：

```python
get_account(source_id: int) -> ArticleAccountConfigRecord | None
set_account_enabled(source_id: int, enabled: bool) -> int
delete_account(source_id: int) -> int
```

`MysqlSourceReferenceRepo` 增加：

```python
list_referencing_jobs(source_type: str, source_id: int, active_only: bool) -> list[str]
has_group_history(group_name: str) -> bool
has_article_history(account_name: str) -> bool
```

`active_only=True` 只检查 `scheduled/active/stop_requested`；删除使用 `active_only=False` 检查所有 target 行。

- [ ] **Step 5: 实现 Service 命令和异常**

`app/services/source_management_service.py` 定义：

```python
@dataclass(frozen=True)
class GroupSourceCommand:
    group_name: str
    is_core_group: bool
    priority: int
    poll_interval_seconds: int
    backtrack_pages: int
    extra_backtrack_pages: int
    remark: str | None


@dataclass(frozen=True)
class ArticleSourceCommand:
    account_name: str
    account_type: str
    priority: int
    poll_interval_minutes: int
    daily_window_start: str
    daily_window_end: str
    max_articles_per_round: int
    collect_today_only: bool
    remark: str | None
```

Service 校验 priority 为 1 到 100、群间隔至少 30 秒、公众号默认间隔至少 10 分钟、回溯值非负、文章上限 1 到 20。删除时先确认记录存在且 disabled，再检查所有引用，最后调用 Repo DELETE；数据库外键冲突转换为 `SourceInUseError`。

- [ ] **Step 6: 运行名单测试并提交**

Run:

```powershell
pytest -q tests/test_group_config_repo.py tests/test_article_account_config_repo.py tests/test_source_management_service.py
```

Expected: PASS。

```powershell
git add app/storage/group_repo.py app/storage/article_config_repo.py app/storage/source_reference_repo.py app/services/source_management_service.py tests/test_group_config_repo.py tests/test_article_account_config_repo.py tests/test_source_management_service.py
git commit -m "feat: add safe source management"
```

### Task 3: 建设名单管理页面

**Files:**
- Create: `app/web/routes/sources.py`
- Create: `app/web/templates/sources/groups.html`
- Create: `app/web/templates/sources/group_form.html`
- Create: `app/web/templates/sources/articles.html`
- Create: `app/web/templates/sources/article_form.html`
- Modify: `app/web/app.py`
- Modify: `app/web/templates/base.html`
- Create: `tests/test_web_sources.py`

**Interfaces:**
- Consumes: `SourceManagementService`。
- Produces: `/sources/groups`、`/sources/articles` 的列表、表单、启停和删除操作。

- [ ] **Step 1: 写 Web 失败测试**

```python
def test_group_list_is_protected(authenticated_client) -> None:
    response = authenticated_client.get("/sources/groups")
    assert response.status_code == 200
    assert "核心群A" in response.text


def test_delete_in_use_source_returns_409_fragment(authenticated_client) -> None:
    response = authenticated_client.post(
        "/sources/groups/7/delete",
        headers={"X-CSRF-Token": authenticated_client.cookies["weinsight_csrf"]},
    )
    assert response.status_code == 409
    assert "核心群晨间采集" in response.text
```

增加公众号 9 分钟被拒绝、已禁用且无引用可删除、未经登录重定向测试。

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
pytest -q tests/test_web_sources.py
```

Expected: FAIL，路由不存在。

- [ ] **Step 3: 实现路由**

路由固定使用 ID：

```python
GET  /sources/groups
GET  /sources/groups/new
POST /sources/groups
GET  /sources/groups/{source_id}/edit
POST /sources/groups/{source_id}
POST /sources/groups/{source_id}/enable
POST /sources/groups/{source_id}/disable
POST /sources/groups/{source_id}/delete
```

公众号使用 `/sources/articles` 对应路径。Service 异常映射：不存在返回 404，校验错误返回 422，引用冲突返回 409。

- [ ] **Step 4: 实现模板交互**

模板必须：

1. 群和公众号使用独立导航项。
2. 删除按钮仅对 disabled 行显示。
3. 删除前显示“只删除配置，不删除历史采集结果”。
4. 冲突响应显示引用任务名称。
5. 所有 POST 表单包含 CSRF hidden input。

- [ ] **Step 5: 运行 Web 测试并提交**

Run:

```powershell
pytest -q tests/test_web_sources.py tests/test_source_management_service.py
```

Expected: PASS。

```powershell
git add app/web/routes/sources.py app/web/templates/sources app/web/app.py app/web/templates/base.html tests/test_web_sources.py
git commit -m "feat: add source management pages"
```

### Task 4: 实现安全结果查询层

**Files:**
- Create: `app/domain/admin_results.py`
- Create: `app/storage/safe_result_query_repo.py`
- Create: `app/services/result_query_service.py`
- Create: `tests/test_safe_result_query_repo.py`
- Create: `tests/test_result_query_service.py`

**Interfaces:**
- Consumes: group clean/analysis、article analysis、egg price item。
- Produces: `PagedResult[T]`、`list_group_details`、`list_article_details`、`list_price_details`。

- [ ] **Step 1: 写安全 SQL 失败测试**

```python
def test_group_detail_query_uses_clean_and_analysis_only(fake_engine) -> None:
    repo = MysqlSafeResultQueryRepo(fake_engine)
    repo.list_group_details(GroupDetailFilter(), page=1, page_size=20)
    sql = fake_engine.last_sql
    assert "wechat_group_msg_clean" in sql
    assert "wechat_group_msg_analysis" in sql
    assert "wechat_group_msg_raw" not in sql
    assert "raw_content" not in sql


def test_article_detail_query_excludes_url_and_body(fake_engine) -> None:
    repo = MysqlSafeResultQueryRepo(fake_engine)
    repo.list_article_details(ArticleDetailFilter(), page=1, page_size=20)
    sql = fake_engine.last_sql
    for forbidden in ("article_url", "body_text", "html_content"):
        assert forbidden not in sql
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
pytest -q tests/test_safe_result_query_repo.py tests/test_result_query_service.py
```

Expected: FAIL，查询层不存在。

- [ ] **Step 3: 定义安全 DTO**

`app/domain/admin_results.py` 定义：

```python
@dataclass(frozen=True)
class PagedResult(Generic[T]):
    items: list[T]
    page: int
    page_size: int
    total_count: int


@dataclass(frozen=True)
class GroupDetailRow:
    msg_hash: str
    group_name: str
    sender_display: str | None
    msg_time_inferred: datetime | None
    clean_content: str
    intent_type: str
    region_hits: tuple[str, ...]
    category_hits: tuple[str, ...]
    keyword_hits: tuple[str, ...]
    opportunity_score: int
    has_contact: bool


@dataclass(frozen=True)
class ArticleDetailRow:
    article_hash: str
    account_name: str
    title: str
    publish_time: datetime | None
    quote_date: date | None
    collect_time: datetime | None
    summary_text: str
    topic_tags: tuple[str, ...]
    content_length: int
    analysis_version: str


@dataclass(frozen=True)
class GroupDetailFilter:
    group_name: str | None = None
    start_at: datetime | None = None
    end_at: datetime | None = None
    intent_type: str | None = None


@dataclass(frozen=True)
class ArticleDetailFilter:
    account_name: str | None = None
    publish_date: date | None = None
    quote_date: date | None = None


@dataclass(frozen=True)
class PriceDetailFilter:
    account_name: str | None = None
    quote_date: date | None = None
    region: str | None = None
    product_family: str | None = None
```

另定义 `EggPriceDetailRow`，字段严格对应设计中的地区、市场、产品、规格、原始报价、标准价、趋势、转换方法和置信度，不包含 URL 或正文。

- [ ] **Step 4: 实现分页 Repo 与 Service**

Repo 方法：

```python
list_group_details(filters: GroupDetailFilter, page: int, page_size: int) -> PagedResult[GroupDetailRow]
list_article_details(filters: ArticleDetailFilter, page: int, page_size: int) -> PagedResult[ArticleDetailRow]
list_price_details(filters: PriceDetailFilter, page: int, page_size: int) -> PagedResult[EggPriceDetailRow]
```

Service 在调用 Repo 前执行：

```python
if page < 1:
    raise ValueError("page must be greater than 0")
if page_size < 1 or page_size > 100:
    raise ValueError("page_size must be between 1 and 100")
```

JSON 列转换失败时返回空 tuple 并记录安全告警，不把原始 JSON 文本回传页面。

- [ ] **Step 5: 运行结果查询测试并提交**

Run:

```powershell
pytest -q tests/test_safe_result_query_repo.py tests/test_result_query_service.py
```

Expected: PASS。

```powershell
git add app/domain/admin_results.py app/storage/safe_result_query_repo.py app/services/result_query_service.py tests/test_safe_result_query_repo.py tests/test_result_query_service.py
git commit -m "feat: add safe result query layer"
```

### Task 5: 建设结果、日报与基础总览页面

**Files:**
- Modify: `requirements.txt`
- Create: `app/security/markdown.py`
- Create: `app/web/routes/results.py`
- Create: `app/web/routes/reports.py`
- Create: `app/web/routes/dashboard.py`
- Create: `app/web/templates/results/groups.html`
- Create: `app/web/templates/results/articles.html`
- Create: `app/web/templates/results/prices.html`
- Create: `app/web/templates/reports/index.html`
- Create: `app/web/templates/dashboard.html`
- Create: `app/web/static/vendor/htmx.min.js`
- Create: `app/web/static/vendor/echarts.min.js`
- Modify: `app/web/app.py`
- Modify: `app/web/templates/base.html`
- Create: `tests/test_web_results.py`
- Create: `tests/test_web_reports.py`
- Create: `tests/test_safe_markdown.py`

**Interfaces:**
- Consumes: `ResultQueryService` 和现有三类日报 QueryService。
- Produces: `/results/groups`、`/results/articles`、`/results/prices`、`/reports`、`/dashboard`。

- [ ] **Step 1: 固定 Markdown 依赖并写安全失败测试**

在 `requirements.txt` 追加：

```text
markdown-it-py==3.0.0
bleach==6.2.0
```

`tests/test_safe_markdown.py` 增加：

```python
def test_markdown_renderer_removes_script_and_event_handlers() -> None:
    rendered = render_safe_markdown(
        "# 日报\n<script>alert(1)</script><img src=x onerror=alert(2)>"
    )
    assert "<h1>日报</h1>" in rendered
    assert "script" not in rendered.lower()
    assert "onerror" not in rendered.lower()
```

`app/security/markdown.py` 使用 `MarkdownIt("commonmark", {"html": False})`，再用 Bleach 只允许标题、段落、列表、强调、代码、表格和 blockquote 标签，不允许事件属性、style、iframe 或 script。

页面测试增加：

```python
def test_group_results_render_clean_content_only(authenticated_client) -> None:
    response = authenticated_client.get("/results/groups?page=1&page_size=20")
    assert response.status_code == 200
    assert "脱敏内容" in response.text
    assert "raw_content" not in response.text


def test_article_results_do_not_render_original_link(authenticated_client) -> None:
    response = authenticated_client.get("/results/articles")
    assert response.status_code == 200
    assert "article_url" not in response.text
    assert "打开原文" not in response.text
```

增加 `page_size=101` 返回 422、日报 Markdown 中 `<script>` 被转义、匿名访问被阻断测试。

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
pytest -q tests/test_safe_markdown.py tests/test_web_results.py tests/test_web_reports.py
```

Expected: FAIL，页面不存在。

- [ ] **Step 3: 本地化 HTMX 与 ECharts**

从固定版本发行文件复制到静态目录：

```powershell
New-Item -ItemType Directory -Force app/web/static/vendor | Out-Null
Invoke-WebRequest https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js -OutFile app/web/static/vendor/htmx.min.js
Invoke-WebRequest https://cdn.jsdelivr.net/npm/echarts@5.6.0/dist/echarts.min.js -OutFile app/web/static/vendor/echarts.min.js
```

`base.html` 只能引用 `/static/vendor/htmx.min.js` 和 `/static/vendor/echarts.min.js`，不得引用外部 URL。提交前人工核对许可证头并在 `README.md` 记录版本。

- [ ] **Step 4: 实现路由与模板**

每个结果页使用 GET 查询参数构造 filter，模板显示服务端分页结果。日报页复用现有 QueryService，并使用 HTML disabled 的 Markdown renderer；渲染后的允许标签限定为标题、段落、列表、表格、强调和代码，不允许 script、style、iframe、事件属性或原始链接自动化。

基础 dashboard 只展示现有 runtime metrics、配置数量和日报数量；Worker 与微信实时卡片在阶段三接入。

- [ ] **Step 5: 运行页面测试并完整回归**

Run:

```powershell
pytest -q tests/test_safe_markdown.py tests/test_web_results.py tests/test_web_reports.py
pytest -q
```

Expected: 全部 PASS。

- [ ] **Step 6: 提交阶段二页面**

```powershell
git add requirements.txt app/security/markdown.py app/web/routes app/web/templates app/web/static/vendor app/web/app.py tests/test_safe_markdown.py tests/test_web_results.py tests/test_web_reports.py README.md
git commit -m "feat: add safe results and report pages"
```
