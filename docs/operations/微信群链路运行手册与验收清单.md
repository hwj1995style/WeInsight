# 微信群链路运行手册与验收清单

## 1. 适用范围

本文档适用于 WeInsight 微信群链路的开发、POC 和小规模试运行。

当前范围只包含微信群链路：

```text
微信群采集 -> raw 入库 -> clean 清洗 -> 规则分析 -> 群日报草稿 -> 状态巡检
```

本文档不启动公众号/订阅号真实采集，不注册 Windows 计划任务。开发环境继续采用人工命令执行和人工值守。

## 2. 环境前置检查

进入项目目录：

```powershell
cd D:\Projects\WeInsight
```

设置开发库密码：

```powershell
$env:WEINSIGHT_MYSQL_PASSWORD='weinsight_dev'
```

确认测试通过：

```powershell
pytest -q
```

确认微信 PC 环境：

```powershell
python -m app.main wechat-health --config config/config.dev.yaml
```

验收标准：

```text
wechat_health_status=ok
version=4.1.8.107
```

如果只执行 `--skip-collect` 后处理流水线，可以不打开微信窗口；如果执行真实采集，必须确认微信已登录且只操作授权测试群。

生产配置模板检查：

```powershell
Copy-Item config\config.prod.example.yaml config\config.prod.yaml
$env:WEINSIGHT_MYSQL_PASSWORD='<生产库密码>'
python -m app.main check-config --config config/config.prod.yaml
```

生产模板必须只通过环境变量提供 MySQL 密码，并保留核心群上限 5、公众号/订阅号上限 20、日志目录 `runtime/logs`、截图目录 `runtime/screenshots`。

## 3. 初始化与配置

初始化开发库表结构：

```powershell
cmd /c "docker exec -i pulsebrief-mysql mysql -uweinsight -pweinsight_dev weinsight_dev < sql\init.sql"
```

已有开发库执行增量升级：

```powershell
Get-ChildItem sql\migrations\*.sql | Sort-Object Name
cmd /c "docker exec -i pulsebrief-mysql mysql -uweinsight -pweinsight_dev weinsight_dev < sql\migrations\20260703_001_add_group_analysis_quality_fields.sql"
```

执行原则：全新库执行 `sql/init.sql`；已有库只按文件名顺序执行尚未应用的 `sql/migrations/*.sql`。迁移脚本必须幂等，禁止直接删除或重建业务表。

查看群配置：

```powershell
python -m app.main group-config-list --config config/config.dev.yaml
```

配置授权核心群：

```powershell
python -m app.main group-config-upsert --config config/config.dev.yaml --group-name "授权测试群名称" --core --priority 1 --poll-interval-seconds 30 --backtrack-pages 1 --extra-backtrack-pages 3
```

禁用群采集：

```powershell
python -m app.main group-config-disable --config config/config.dev.yaml --group-name "授权测试群名称"
```

验收标准：

```text
只配置已授权群。
核心群生产初版不超过 5 个。
开发环境不注册 Windows 计划任务。
```

## 4. 手动运行流程

安全后处理流水线，不打开微信 UI：

```powershell
python -m app.main run-group-pipeline-once --config config/config.dev.yaml --date 2026-07-03 --skip-collect --limit 20 --rules-config config/group_analysis_rules.yaml
```

真实单群流水线，会打开微信窗口：

```powershell
python -m app.main run-group-pipeline-once --config config/config.dev.yaml --date 2026-07-03 --group-name "授权测试群名称" --limit 20 --rules-config config/group_analysis_rules.yaml
```

拆分执行时，按以下顺序：

```powershell
python -m app.main collect-group-once --config config/config.dev.yaml --group-name "授权测试群名称"
python -m app.main clean-group-once --config config/config.dev.yaml --limit 50
python -m app.main analyze-group-once --config config/config.dev.yaml --rules-config config/group_analysis_rules.yaml --limit 100
python -m app.main group-daily-report-once --config config/config.dev.yaml --date 2026-07-03
python -m app.main group-daily-report-export --config config/config.dev.yaml --date 2026-07-03 --group-name "授权测试群名称" --output runtime/reports/group
```

验收标准：

```text
采集阶段必须显式传入授权群名。
清洗、分析、日报阶段不占用微信 UI。
流水线任一阶段失败时停止后续阶段。
命令不输出消息正文。
```

群链路长稳启动脚本：

```powershell
$env:WEINSIGHT_MYSQL_PASSWORD='weinsight_dev'
.\scripts\windows\start_group_scheduler.ps1 -Once
.\scripts\windows\start_group_scheduler.ps1
```

日志位置：

```text
runtime\logs\group_scheduler\group_scheduler_YYYYMMDD.log
```

验收标准：

```text
脚本启动时记录 ProjectRoot、ConfigPath、Once 和日志轮转提示。
脚本退出时记录 exit_code。
exit_code 非 0 时输出异常退出提示。
开发环境不执行 register_group_scheduler_task.ps1。
只有生产试运行前，才允许人工注册 Windows 计划任务。
```

## 5. 巡检与验收清单

查看运行状态汇总：

```powershell
python -m app.main group-runtime-summary --config config/config.dev.yaml --limit 5
```

查看试运行监控指标：

```powershell
python -m app.main group-runtime-metrics --config config/config.dev.yaml --hours 24
```

查看公众号/订阅号 POC 监控指标：

```powershell
python -m app.main article-runtime-metrics --config config/config.dev.yaml --hours 24
```

该命令只读 article 配置、采集日志和任务表，显示账号数量、采集成功数、失败数、跳过数、任务积压和最近错误摘要；不打开微信 UI，不影响群链路轮询。

查看单群状态：

```powershell
python -m app.main group-status --config config/config.dev.yaml --group-name "授权测试群名称"
```

查看日报摘要：

```powershell
python -m app.main group-daily-report-list --config config/config.dev.yaml --date 2026-07-03 --limit 20
```

查看日报正文：

```powershell
python -m app.main group-daily-report-show --config config/config.dev.yaml --date 2026-07-03 --group-name "授权测试群名称"
```

执行敏感输出回归检查：

```powershell
pytest tests/test_sensitive_output_guard.py -q
```

验收清单：

```text
[ ] pytest -q 通过。
[ ] wechat-health 返回 ok。
[ ] group-runtime-summary 显示 ui_lock status=free 或锁 owner 明确。
[ ] group-runtime-metrics 显示最近 24 小时采集成功数、失败数、失败率、任务积压和日报生成数。
[ ] article-runtime-metrics 显示账号数、采集成功数、失败数、跳过数、任务积压和最近错误摘要。
[ ] clean_group_msg 无 failed 积压。
[ ] analyze_group_msg 无 failed 积压。
[ ] group_daily_report 已生成。
[ ] 日报正文包含可疑商机数、地区 TOP、品类 TOP、商机词 TOP 和联系方式概览。
[ ] group-daily-report-export 生成 Markdown 文件。
[ ] CLI 输出不包含敏感正文列名。
[ ] 导出的 Markdown 日报不包含 raw 表字段。
[ ] 日志和截图路径可追踪失败采集。
```

## 6. 故障恢复

微信健康检查失败：

```text
1. 确认微信 PC 已登录。
2. 确认微信版本为 4.1.8.107。
3. 确认微信自动更新已关闭。
4. 重新运行 wechat-health。
```

UI 锁未释放：

```text
1. 先运行 group-runtime-summary 查看 owner_pipeline 和 expire_time。
2. 如果未过期，等待锁租约到期。
3. 如果反复卡死，先人工确认没有正在执行的采集命令，再处理锁记录。
```

清洗或分析任务失败：

```powershell
python -m app.main group-runtime-summary --config config/config.dev.yaml --limit 5
python -m app.main group-task-failed-list --config config/config.dev.yaml --limit 20
python -m app.main group-task-retry-failed --config config/config.dev.yaml --task-type analyze_group_msg --limit 5
python -m app.main group-task-reset --config config/config.dev.yaml --task-type analyze_group_msg --ref-id "消息hash"
```

处理原则：先保留失败现场，不直接删除任务；批量重试必须使用 `--limit` 小步执行。需要单条补偿时，确认 `task_type/ref_id` 后再重置。`clean_group_msg` 和 `analyze_group_msg` 的 `ref_id` 是消息 hash。

日报未生成：

```powershell
python -m app.main group-task-reset-date --config config/config.dev.yaml --date 2026-07-03
python -m app.main group-daily-report-once --config config/config.dev.yaml --date 2026-07-03
python -m app.main group-daily-report-list --config config/config.dev.yaml --date 2026-07-03
```

补偿命令边界：

```text
group-task-list 只读 wechat_group_process_task。
group-task-reset 只允许 clean_group_msg/analyze_group_msg/group_daily_report。
group-task-reset-date 只创建或重置 group_daily_report(date)。
group-task-failed-list 只查看 status=failed 的群任务死信队列。
group-task-retry-failed 只限量把 failed 群任务重置为 pending。
补偿命令不占用微信 UI，不读写 wechat_article_process_task，不输出消息正文。
```

## 7. 禁止事项

```text
禁止采集未授权微信群。
禁止在开发环境注册 Windows 计划任务。
禁止绕过 wechat_ui_lock 做并发微信 UI 操作。
禁止在 CLI、日志、日报摘要中输出 raw 原文或 clean 消息正文。
禁止从群链路命令读写 wechat_article_process_task。
禁止为了恢复任务直接清空任务表。
```

本阶段所有命令必须默认不输出消息正文。需要排查原文时，只能在授权范围内由人工按 hash 回查数据库。
