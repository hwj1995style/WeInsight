# WeRSS 正文按需读取 POC 记录

## 状态与冻结范围

- 24 小时观察状态：未启动（阻断中）
- 首轮公众号：湖南三尖农牧公司
- WeRSS Feed ID：`MP_WXS_3545051769`
- 当前范围：1；目标总数：9
- 扩容门槛：严格按 1 → 3 → 9，当前观察结束前不扩容
- 已接受风险：官方固定镜像会把 SQL 参数/正文写入仅本机、2 × 2 MB 短轮转的 Docker 日志；不构建内部镜像，不外传日志，并持续关注上游开关。
- 当前阻断：真实 shadow 长度/hash 不一致已解释为网页侧仅返回 65 字符受限降级响应，并非两份完整正文不一致；WeRSS 正文 1278 字符且结构化分析成功。不伪造等值，保持 `content_mode: shadow`，等待复审，不得切换或开始 24 小时计时。

## 双层范围与验收

- 采集目标：9 个公众号全部启用采集。
- 下游目标：仅湖南三尖农牧公司进入 clean/analyze。
- 隔离要求：其余 8 个只采集，不创建或消费 clean/analyze 任务。
- 当前实现状态：9 个目标均已启用采集，只有湖南 `downstream_clean_enabled=1`；本轮其余 8 个没有新增 clean/analyze 任务。
- 分开验收：9 账号记录采集完整率、去重、采集延迟；湖南单独记录正文成功率、回退、失败恢复和分析结果。
- 映射核对：WeRSS 已有 9 个 Feed；“江西九江褐壳蛋”已确认与 WeRSS 映射一致。

最终九个公众号固定为：河南金咕咕蛋品、江西九江褐壳蛋、成都鸡蛋价格、蓝天禽蛋联盟、贵阳鸡蛋价格、河北辛集城方蛋品、湖南三尖农牧公司、河北馆陶鸡蛋报价、家美鲜鸡蛋 佳美鲜。

## 变更与观察窗口

| 项目 | 记录 |
| --- | --- |
| 代码提交 | 待填写 |
| 固定镜像摘要 | `sha256:53912fcb3d523d1e640adcb7066cc18123f00e9510882a7982d0991f3113845f` |
| 正文接口契约验证 | 2026-07-12：湖南 Feed/正文/article 路由可用，25/25 locator |
| shadow 开始时间 | 2026-07-12 12:32 +08:00（对账发现差异，未进入观察窗口） |
| werss_first 开始时间 | 待满足影子门槛后填写 |
| 24 小时截止时间 | 待开始后填写 |
| 基线计数 | 湖南 clean 25/25、analyze 25/25、analysis 25；article UI lock 0 |

## 影子样本对账

只记录安全元数据；不得粘贴文章正文、原始 HTML、Cookie、令牌或完整敏感 URL。

| 样本安全标识 | WeRSS 正文长度 | 网页正文长度 | 内容哈希是否一致 | 结构化报价差异 | 解释与处置 |
| --- | ---: | ---: | --- | --- | --- |
| 最新真实样本（不记录标识） | 1278 | 65 | 否 | WeRSS 结构化分析成功；web 不作为等值基线 | web 为受限降级响应；保留长度/hash 差异并记录 `shadow_web_limited_response_count`，等待复审 |

## 故障与恢复演练

| 检查 | 结果 |
| --- | --- |
| 停止 WeRSS 后网页回退 | 待验证 |
| 恢复后容器状态 healthy | 待验证 |
| 下一任务来源回到 WeRSS | 待验证 |
| article UI lock 计数为 0 | 待验证 |
| 数据库与日志无正文 | 待验证 |

## 观察计数

| 时间 | 正文成功 | 网页回退次数 | 正文失败次数 | 正文任务积压 | clean/analyze 状态 | 备注 |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| 2026-07-12 12:40 +08:00 | shadow 最终来源 web：25 | 未进入 werss_first | 0 个任务失败 | clean 0、analyze 0 | clean/analyze 各 25 success | 24 小时窗口未启动 |

## 回滚

任一阻断项出现时，将该公众号恢复为 `content_mode: web`，暂停扩大范围；记录结构化错误码和安全计数。未完成连续 24 小时观察前，本记录不作验收通过声明。
